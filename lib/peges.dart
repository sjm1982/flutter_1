import 'package:flutter/material.dart';




class Explorpage extends StatelessWidget {
  const Explorpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amberAccent,
    );
  }
}
class Pasttripstpage extends StatelessWidget {
  const Pasttripstpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrange,
    );
  }
}



